# Sub-repos (independent git repos, co-located by convention)
follow-api/
follow-image-gateway/
follow-app/
follow-pkg/
follow-business/
follow-app-worktrees/

# Environment (sensitive values)
.env
# Exception: the integration test env file contains dummy credentials
# only and is required for docker-mode runs and CI.
!tests/integration/.env

# OS
.DS_Store

# Serena
.serena/

custom-gcl
