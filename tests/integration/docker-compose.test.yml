# Test-only compose override — loaded on top of the root
# docker-compose.yml when the cross-repo integration tests start the
# stack in docker mode. Mirrors the env vars that setupLocal() sets on
# the follow-api subprocess, so docker mode exercises the same API
# behavior as local mode.
#
# This file intentionally does NOT redeclare the base service config —
# Docker Compose merges list-form `environment:` entries by variable
# name, so the base service's image/build/depends_on/ports/healthcheck
# are preserved and only the listed vars are added or overridden.
#
# IMPORTANT: do not add production-style settings here. This file is
# for semantics that must differ between "dev stack" and "integration
# test run" — e.g. disabling rate limiting so a test suite can hit the
# anonymous-user endpoint hundreds of times without tripping 429s.

services:
  mailpit:
    image: axllent/mailpit:latest
    container_name: ${MAILPIT_CONTAINER_NAME:-follow-mailpit-test}
    restart: unless-stopped
    ports:
      - "127.0.0.1:${MAILPIT_SMTP_HOST_PORT:-1025}:1025"
      - "127.0.0.1:${MAILPIT_API_HOST_PORT:-8025}:8025"
    networks:
      - internal
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "5"
    deploy:
      resources:
        limits:
          memory: 64m
          cpus: "0.25"
    healthcheck:
      test: ["CMD", "wget", "-q", "-O", "/dev/null", "http://localhost:8025/api/v1/messages"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

  follow-api:
    depends_on:
      mailpit:
        condition: service_healthy
    deploy:
      resources:
        limits:
          memory: 512m
    environment:
      # Tests hit POST /api/v1/users/anonymous many times in a single
      # run. Rate limiting would cause spurious 429s in tests that are
      # unrelated to rate-limit behavior.
      - RATE_LIMIT_ENABLED=false
      # Reclaimer tuned aggressively so tests that exercise pending/
      # stuck-message recovery don't have to wait minutes.
      - RECLAIMER_IDLE_TIMEOUT=5s
      - RECLAIMER_SCAN_INTERVAL=2s
      - REAPER_SCAN_INTERVAL=1s
      # Gateway stages (ML inference) take 3-8s under container
      # resource limits (cpus: 2.0). Local mode has no limits so 2s
      # works there. 15s accommodates the slowest observed stage.
      - REAPER_STALE_THRESHOLD=15s
      # SMTP — routed to Mailpit for test interception.
      # SMTP_USERNAME and SMTP_PASSWORD must be empty so the
      # API sends unauthenticated SMTP; Mailpit doesn't
      # support AUTH and rejects credentials.
      - SMTP_HOST=mailpit
      - SMTP_PORT=1025
      - SMTP_USERNAME=
      - SMTP_PASSWORD=
      - SMTP_FROM=noreply@follow.app
      # Short resend cooldown so verification-code tests
      # don't wait 60s between register and resend.
      - AUTH_RESEND_COOLDOWN=5s
      # Fast argon2id for tests (OWASP minimum — still
      # validates the flow but ~10x faster than production).
      - AUTH_ARGON2ID_MEMORY=15360
      - AUTH_ARGON2ID_ITERATIONS=1

