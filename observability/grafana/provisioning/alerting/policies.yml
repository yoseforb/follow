apiVersion: 1

policies:
  - orgId: 1
    receiver: follow-email
    group_by:
      - grafana_folder
      - alertname
    group_wait: 30s
    group_interval: 5m
    repeat_interval: 4h
    routes:
      - receiver: follow-telegram
        matchers:
          - severity = critical
        group_wait: 15s
        group_interval: 2m
        repeat_interval: 1h
        continue: true
      - receiver: follow-email
        matchers:
          - severity = critical
        group_wait: 15s
        group_interval: 2m
        repeat_interval: 1h
        continue: false
