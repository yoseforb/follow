{
  "annotations": { "list": [] },
  "editable": true,
  "graphTooltip": 1,
  "links": [],
  "panels": [
    {
      "collapsed": false,
      "gridPos": { "h": 1, "w": 24, "x": 0, "y": 0 },
      "id": 100,
      "title": "HTTP Layer",
      "type": "row"
    },
    {
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "fieldConfig": {
        "defaults": {
          "unit": "reqps",
          "decimals": 1,
          "color": { "mode": "thresholds" },
          "thresholds": { "steps": [{ "color": "green", "value": null }] }
        }
      },
      "gridPos": { "h": 6, "w": 6, "x": 0, "y": 1 },
      "id": 1,
      "options": { "reduceOptions": { "calcs": ["lastNotNull"] } },
      "title": "HTTP RPS",
      "type": "stat",
      "targets": [
        {
          "expr": "sum(rate(follow_gateway_http_requests_total[5m]))",
          "legendFormat": "rps"
        }
      ]
    },
    {
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "fieldConfig": {
        "defaults": {
          "unit": "s",
          "custom": { "drawStyle": "line", "fillOpacity": 10 }
        }
      },
      "gridPos": { "h": 6, "w": 12, "x": 6, "y": 1 },
      "id": 2,
      "title": "HTTP Latency (p50 / p95 / p99)",
      "type": "timeseries",
      "targets": [
        {
          "expr": "histogram_quantile(0.50, sum(rate(follow_gateway_http_request_duration_seconds_bucket[5m])) by (le))",
          "legendFormat": "p50"
        },
        {
          "expr": "histogram_quantile(0.95, sum(rate(follow_gateway_http_request_duration_seconds_bucket[5m])) by (le))",
          "legendFormat": "p95"
        },
        {
          "expr": "histogram_quantile(0.99, sum(rate(follow_gateway_http_request_duration_seconds_bucket[5m])) by (le))",
          "legendFormat": "p99"
        }
      ]
    },
    {
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "fieldConfig": {
        "defaults": {
          "unit": "percentunit",
          "color": { "mode": "thresholds" },
          "thresholds": {
            "steps": [
              { "color": "green", "value": null },
              { "color": "yellow", "value": 0.01 },
              { "color": "red", "value": 0.05 }
            ]
          }
        }
      },
      "gridPos": { "h": 6, "w": 6, "x": 18, "y": 1 },
      "id": 3,
      "options": { "reduceOptions": { "calcs": ["lastNotNull"] } },
      "title": "HTTP Error Rate",
      "type": "stat",
      "targets": [
        {
          "expr": "sum(rate(follow_gateway_http_requests_total{status_code=~\"5..\"}[5m])) / sum(rate(follow_gateway_http_requests_total[5m]))",
          "legendFormat": "error_rate"
        }
      ]
    },
    {
      "collapsed": false,
      "gridPos": { "h": 1, "w": 24, "x": 0, "y": 7 },
      "id": 101,
      "title": "Pipeline",
      "type": "row"
    },
    {
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "fieldConfig": {
        "defaults": {
          "unit": "short",
          "decimals": 0,
          "color": { "mode": "thresholds" },
          "thresholds": {
            "steps": [
              { "color": "green", "value": null },
              { "color": "yellow", "value": 5 },
              { "color": "red", "value": 9 }
            ]
          }
        }
      },
      "gridPos": { "h": 6, "w": 4, "x": 0, "y": 8 },
      "id": 4,
      "options": { "reduceOptions": { "calcs": ["lastNotNull"] } },
      "title": "Jobs In Flight",
      "type": "stat",
      "targets": [
        { "expr": "follow_gateway_pipeline_jobs_in_flight", "legendFormat": "in flight" }
      ]
    },
    {
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "fieldConfig": {
        "defaults": {
          "unit": "short",
          "decimals": 0,
          "color": { "mode": "thresholds" },
          "thresholds": {
            "steps": [
              { "color": "green", "value": null },
              { "color": "yellow", "value": 5 },
              { "color": "red", "value": 9 }
            ]
          }
        }
      },
      "gridPos": { "h": 6, "w": 4, "x": 4, "y": 8 },
      "id": 5,
      "options": { "reduceOptions": { "calcs": ["lastNotNull"] } },
      "title": "Queue Depth",
      "type": "stat",
      "targets": [
        { "expr": "follow_gateway_pipeline_queue_depth", "legendFormat": "queued" }
      ]
    },
    {
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "fieldConfig": {
        "defaults": { "unit": "s", "custom": { "drawStyle": "line", "fillOpacity": 10 } }
      },
      "gridPos": { "h": 6, "w": 8, "x": 8, "y": 8 },
      "id": 6,
      "title": "Stage Duration p95",
      "type": "timeseries",
      "targets": [
        {
          "expr": "histogram_quantile(0.95, sum(rate(follow_gateway_pipeline_stage_duration_seconds_bucket[5m])) by (le, stage))",
          "legendFormat": "{{stage}}"
        }
      ]
    },
    {
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "fieldConfig": {
        "defaults": { "unit": "ops", "custom": { "drawStyle": "line" } }
      },
      "gridPos": { "h": 6, "w": 8, "x": 16, "y": 8 },
      "id": 7,
      "title": "Jobs Completed / sec",
      "type": "timeseries",
      "targets": [
        {
          "expr": "sum(rate(follow_gateway_pipeline_jobs_completed_total[5m])) by (status)",
          "legendFormat": "{{status}}"
        }
      ]
    },
    {
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "fieldConfig": {
        "defaults": { "unit": "ops", "custom": { "drawStyle": "line" } }
      },
      "gridPos": { "h": 6, "w": 8, "x": 0, "y": 14 },
      "id": 8,
      "title": "Stage Errors / sec",
      "type": "timeseries",
      "targets": [
        {
          "expr": "sum(rate(follow_gateway_pipeline_stage_errors_total[5m])) by (stage, error_code)",
          "legendFormat": "{{stage}}: {{error_code}}"
        }
      ]
    },
    {
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "fieldConfig": {
        "defaults": {
          "unit": "ops",
          "decimals": 1,
          "color": { "mode": "thresholds" },
          "thresholds": {
            "steps": [
              { "color": "green", "value": null },
              { "color": "red", "value": 1 }
            ]
          }
        }
      },
      "gridPos": { "h": 6, "w": 4, "x": 8, "y": 14 },
      "id": 9,
      "options": { "reduceOptions": { "calcs": ["lastNotNull"] } },
      "title": "Rejections / sec",
      "type": "stat",
      "targets": [
        {
          "expr": "rate(follow_gateway_pipeline_full_rejections_total[5m])",
          "legendFormat": "rejected"
        }
      ]
    },
    {
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "fieldConfig": {
        "defaults": { "unit": "ops", "custom": { "drawStyle": "line" } }
      },
      "gridPos": { "h": 6, "w": 6, "x": 12, "y": 14 },
      "id": 10,
      "title": "Jobs Dropped / sec",
      "type": "timeseries",
      "targets": [
        {
          "expr": "sum(rate(follow_gateway_pipeline_jobs_dropped_total[5m])) by (stage, reason)",
          "legendFormat": "{{stage}}: {{reason}}"
        }
      ]
    },
    {
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "fieldConfig": {
        "defaults": { "unit": "ops", "custom": { "drawStyle": "bars" } }
      },
      "gridPos": { "h": 6, "w": 6, "x": 18, "y": 14 },
      "id": 11,
      "title": "Upload Submit Outcomes / sec",
      "type": "timeseries",
      "targets": [
        {
          "expr": "sum(rate(follow_gateway_upload_submit_total[5m])) by (result)",
          "legendFormat": "{{result}}"
        }
      ]
    },
    {
      "collapsed": false,
      "gridPos": { "h": 1, "w": 24, "x": 0, "y": 20 },
      "id": 102,
      "title": "Detection",
      "type": "row"
    },
    {
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "fieldConfig": {
        "defaults": { "unit": "s", "custom": { "drawStyle": "line", "fillOpacity": 10 } }
      },
      "gridPos": { "h": 6, "w": 8, "x": 0, "y": 21 },
      "id": 12,
      "title": "Detection Duration p95",
      "type": "timeseries",
      "targets": [
        {
          "expr": "histogram_quantile(0.95, sum(rate(follow_gateway_detection_duration_seconds_bucket[5m])) by (le, detector))",
          "legendFormat": "{{detector}}"
        }
      ]
    },
    {
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "fieldConfig": {
        "defaults": { "unit": "ops", "custom": { "drawStyle": "bars" } }
      },
      "gridPos": { "h": 6, "w": 8, "x": 8, "y": 21 },
      "id": 13,
      "title": "Regions Detected / sec",
      "type": "timeseries",
      "targets": [
        {
          "expr": "sum(rate(follow_gateway_detection_regions_total[5m])) by (detector)",
          "legendFormat": "{{detector}}"
        }
      ]
    },
    {
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "fieldConfig": {
        "defaults": { "unit": "ops", "custom": { "drawStyle": "line" } }
      },
      "gridPos": { "h": 6, "w": 8, "x": 16, "y": 21 },
      "id": 14,
      "title": "Detection Errors / sec",
      "type": "timeseries",
      "targets": [
        {
          "expr": "sum(rate(follow_gateway_detection_errors_total[5m])) by (detector)",
          "legendFormat": "{{detector}}"
        }
      ]
    },
    {
      "collapsed": false,
      "gridPos": { "h": 1, "w": 24, "x": 0, "y": 27 },
      "id": 103,
      "title": "Image Processing",
      "type": "row"
    },
    {
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "fieldConfig": {
        "defaults": { "unit": "decbytes", "custom": { "drawStyle": "line", "fillOpacity": 10 } }
      },
      "gridPos": { "h": 6, "w": 6, "x": 0, "y": 28 },
      "id": 15,
      "title": "Upload Size (p50 / p95)",
      "type": "timeseries",
      "targets": [
        {
          "expr": "histogram_quantile(0.50, sum(rate(follow_gateway_image_upload_size_bytes_bucket[5m])) by (le))",
          "legendFormat": "p50"
        },
        {
          "expr": "histogram_quantile(0.95, sum(rate(follow_gateway_image_upload_size_bytes_bucket[5m])) by (le))",
          "legendFormat": "p95"
        }
      ]
    },
    {
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "fieldConfig": {
        "defaults": { "unit": "decbytes", "custom": { "drawStyle": "line", "fillOpacity": 10 } }
      },
      "gridPos": { "h": 6, "w": 6, "x": 6, "y": 28 },
      "id": 16,
      "title": "Processed Size (p50 / p95)",
      "type": "timeseries",
      "targets": [
        {
          "expr": "histogram_quantile(0.50, sum(rate(follow_gateway_image_processed_size_bytes_bucket[5m])) by (le))",
          "legendFormat": "p50"
        },
        {
          "expr": "histogram_quantile(0.95, sum(rate(follow_gateway_image_processed_size_bytes_bucket[5m])) by (le))",
          "legendFormat": "p95"
        }
      ]
    },
    {
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "fieldConfig": {
        "defaults": { "unit": "short", "custom": { "drawStyle": "line", "fillOpacity": 10 } }
      },
      "gridPos": { "h": 6, "w": 6, "x": 12, "y": 28 },
      "id": 17,
      "title": "Original Megapixels (p50 / p95)",
      "type": "timeseries",
      "targets": [
        {
          "expr": "histogram_quantile(0.50, sum(rate(follow_gateway_image_original_pixels_bucket[5m])) by (le)) / 1e6",
          "legendFormat": "p50"
        },
        {
          "expr": "histogram_quantile(0.95, sum(rate(follow_gateway_image_original_pixels_bucket[5m])) by (le)) / 1e6",
          "legendFormat": "p95"
        }
      ]
    },
    {
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "fieldConfig": {
        "defaults": { "unit": "short", "decimals": 1, "custom": { "drawStyle": "line", "fillOpacity": 10 } }
      },
      "gridPos": { "h": 6, "w": 6, "x": 18, "y": 28 },
      "id": 18,
      "title": "Blur Regions Applied (p50 / p95)",
      "type": "timeseries",
      "targets": [
        {
          "expr": "histogram_quantile(0.50, sum(rate(follow_gateway_image_blur_regions_applied_bucket[5m])) by (le))",
          "legendFormat": "p50"
        },
        {
          "expr": "histogram_quantile(0.95, sum(rate(follow_gateway_image_blur_regions_applied_bucket[5m])) by (le))",
          "legendFormat": "p95"
        }
      ]
    },
    {
      "collapsed": false,
      "gridPos": { "h": 1, "w": 24, "x": 0, "y": 34 },
      "id": 104,
      "title": "Storage & Messaging",
      "type": "row"
    },
    {
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "fieldConfig": {
        "defaults": { "unit": "s", "custom": { "drawStyle": "line" } }
      },
      "gridPos": { "h": 6, "w": 6, "x": 0, "y": 35 },
      "id": 19,
      "title": "MinIO Upload Duration p95",
      "type": "timeseries",
      "targets": [
        {
          "expr": "histogram_quantile(0.95, sum(rate(follow_gateway_storage_upload_duration_seconds_bucket[5m])) by (le, status))",
          "legendFormat": "{{status}}"
        }
      ]
    },
    {
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "fieldConfig": {
        "defaults": { "unit": "ops", "custom": { "drawStyle": "line" } }
      },
      "gridPos": { "h": 6, "w": 6, "x": 6, "y": 35 },
      "id": 20,
      "title": "Valkey Publish Rate",
      "type": "timeseries",
      "targets": [
        {
          "expr": "sum(rate(follow_gateway_messaging_publish_total[5m])) by (status)",
          "legendFormat": "{{status}}"
        }
      ]
    },
    {
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "fieldConfig": {
        "defaults": { "unit": "s", "custom": { "drawStyle": "line" } }
      },
      "gridPos": { "h": 6, "w": 6, "x": 12, "y": 35 },
      "id": 25,
      "title": "Valkey Publish Duration p95",
      "type": "timeseries",
      "targets": [
        {
          "expr": "histogram_quantile(0.95, sum(rate(follow_gateway_messaging_publish_duration_seconds_bucket[5m])) by (le))",
          "legendFormat": "p95"
        }
      ]
    },
    {
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "fieldConfig": {
        "defaults": {
          "unit": "ops",
          "decimals": 1,
          "color": { "mode": "thresholds" },
          "thresholds": {
            "steps": [
              { "color": "green", "value": null },
              { "color": "yellow", "value": 1 },
              { "color": "red", "value": 5 }
            ]
          }
        }
      },
      "gridPos": { "h": 6, "w": 6, "x": 18, "y": 35 },
      "id": 21,
      "options": { "reduceOptions": { "calcs": ["lastNotNull"] } },
      "title": "Progress Retries / sec",
      "type": "stat",
      "targets": [
        {
          "expr": "rate(follow_gateway_messaging_progress_retries_total[5m])",
          "legendFormat": "retries/sec"
        }
      ]
    },
    {
      "collapsed": false,
      "gridPos": { "h": 1, "w": 24, "x": 0, "y": 41 },
      "id": 105,
      "title": "Auth & Service Health",
      "type": "row"
    },
    {
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "fieldConfig": {
        "defaults": { "unit": "ops", "custom": { "drawStyle": "line" } }
      },
      "gridPos": { "h": 6, "w": 12, "x": 0, "y": 42 },
      "id": 22,
      "title": "Auth Token Failures / sec",
      "type": "timeseries",
      "targets": [
        {
          "expr": "sum(rate(follow_gateway_auth_token_failures_total[5m])) by (reason)",
          "legendFormat": "{{reason}}"
        }
      ]
    },
    {
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "fieldConfig": {
        "defaults": {
          "unit": "dateTimeAsIso",
          "color": { "mode": "thresholds" },
          "thresholds": { "steps": [{ "color": "blue", "value": null }] }
        }
      },
      "gridPos": { "h": 6, "w": 4, "x": 12, "y": 42 },
      "id": 24,
      "options": {
        "reduceOptions": { "calcs": ["lastNotNull"] },
        "textMode": "value"
      },
      "title": "Server Started At",
      "description": "Timestamp when the server process started",
      "type": "stat",
      "targets": [
        {
          "expr": "follow_gateway_server_start_time_seconds * 1000",
          "legendFormat": "started"
        }
      ]
    },
    {
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "fieldConfig": {
        "defaults": {
          "unit": "dtdurations",
          "color": { "mode": "thresholds" },
          "thresholds": { "steps": [{ "color": "green", "value": null }] }
        }
      },
      "gridPos": { "h": 6, "w": 8, "x": 16, "y": 42 },
      "id": 23,
      "options": { "reduceOptions": { "calcs": ["lastNotNull"] } },
      "title": "Uptime",
      "type": "stat",
      "targets": [
        {
          "expr": "time() - follow_gateway_server_start_time_seconds",
          "legendFormat": "uptime"
        }
      ]
    }
  ],
  "schemaVersion": 39,
  "tags": ["follow-image-gateway", "observability"],
  "templating": {
    "list": [
      {
        "current": { "selected": false, "text": "Prometheus", "value": "Prometheus" },
        "hide": 0,
        "includeAll": false,
        "name": "DS_PROMETHEUS",
        "options": [],
        "query": "prometheus",
        "type": "datasource"
      }
    ]
  },
  "refresh": "30s",
  "time": { "from": "now-30m", "to": "now" },
  "title": "Follow Image Gateway",
  "uid": "follow-gateway-overview",
  "version": 3
}
