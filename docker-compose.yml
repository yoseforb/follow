# Master Docker Compose — Full-Stack Follow Platform
#
# Combines infrastructure from follow-api and follow-image-gateway into a
# unified development environment. Each sub-repo keeps its own compose file
# for isolated development; this file orchestrates the full stack.
#
# Usage:
#   docker compose up -d              # Start everything
#   docker compose up -d postgres     # Start just the database
#   docker compose logs -f follow-api # Tail API logs
#
# Per-repo isolated development (unchanged):
#   cd follow-api && docker compose up -d
#   cd follow-image-gateway && docker compose up -d
#
# Port override example (for integration tests):
#   POSTGRES_HOST_PORT=15432 VALKEY_HOST_PORT=16379 docker compose up -d

services:
  # ── Infrastructure ─────────────────────────────────────────────────

  postgres:
    image: postgres:17-alpine
    container_name: ${POSTGRES_CONTAINER_NAME:-follow-postgres}
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "${POSTGRES_HOST_PORT:-5432}:5432"
    networks:
      - internal
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "5"
    deploy:
      resources:
        limits:
          memory: 768m
          cpus: "1.0"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s

  valkey:
    image: valkey/valkey:8
    container_name: ${VALKEY_CONTAINER_NAME:-follow-valkey}
    restart: unless-stopped
    command: valkey-server --appendonly yes --maxmemory 256mb --maxmemory-policy noeviction
    volumes:
      - valkey_data:/data
    ports:
      - "${VALKEY_HOST_PORT:-6379}:6379"
    networks:
      - internal
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "5"
    deploy:
      resources:
        limits:
          memory: 128m
          cpus: "0.5"
    healthcheck:
      test: ["CMD", "valkey-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

  minio:
    image: minio/minio:latest
    container_name: ${MINIO_CONTAINER_NAME:-follow-minio}
    restart: unless-stopped
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD}
    volumes:
      - minio_data:/data
    ports:
      - "${MINIO_HOST_PORT:-9000}:9000"
      - "${MINIO_CONSOLE_HOST_PORT:-9001}:9001"
    networks:
      - internal
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "5"
    deploy:
      resources:
        limits:
          memory: 384m
          cpus: "1.0"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 15s

  createbuckets:
    image: minio/mc:latest
    depends_on:
      minio:
        condition: service_healthy
    networks:
      - internal
    environment:
      - MINIO_ROOT_USER=${MINIO_ROOT_USER}
      - MINIO_ROOT_PASSWORD=${MINIO_ROOT_PASSWORD}
    entrypoint: >
      /bin/sh -c "
      mc alias set myminio http://minio:9000 $$MINIO_ROOT_USER $$MINIO_ROOT_PASSWORD;
      mc mb --ignore-existing myminio/follow-images;
      mc version enable myminio/follow-images;
      echo 'Bucket follow-images ready';
      "
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "5"

  # ── Application Services ───────────────────────────────────────────

  follow-api:
    image: follow-api:latest
    build:
      context: ./follow-api
      dockerfile: Dockerfile
      additional_contexts:
        follow-pkg: ./follow-pkg
    container_name: ${API_CONTAINER_NAME:-follow-api}
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
      valkey:
        condition: service_healthy
      minio:
        condition: service_healthy
    environment:
      - SERVER_HOST=0.0.0.0
      - SERVER_PORT=8080
      - POSTGRESQL_URI=postgres://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}?sslmode=${POSTGRES_SSLMODE}
      - VALKEY_ADDRESS=valkey:6379
      - MINIO_ENDPOINT=minio:9000
      - MINIO_EXTERNAL_ENDPOINT=${HOST_IP:-localhost}:${MINIO_HOST_PORT:-9000}
      - MINIO_ACCESS_KEY_ID=${MINIO_ACCESS_KEY_ID}
      - MINIO_SECRET_ACCESS_KEY=${MINIO_SECRET_ACCESS_KEY}
      - MINIO_USE_SSL=${MINIO_USE_SSL}
      - MINIO_BUCKET_NAME=follow-images
      - JWT_SECRET=${JWT_SECRET}
      - FOLLOW_API_ED25519_PRIVATE_KEY=${FOLLOW_API_ED25519_PRIVATE_KEY}
      - GATEWAY_BASE_URL=http://${HOST_IP:-localhost}:${GATEWAY_HOST_PORT:-8090}
    ports:
      - "${API_HOST_PORT:-8080}:8080"
    networks:
      - internal
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "5"
    deploy:
      resources:
        limits:
          memory: 128m
          cpus: "1.0"
    healthcheck:
      # Use GET (-O /dev/null), not --spider: --spider sends HEAD, which
      # Goa-generated routes reject with 405 since /health is GET-only.
      test: ["CMD", "wget", "-q", "-O", "/dev/null", "http://localhost:8080/health"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 5s

  follow-image-gateway:
    image: follow-image-gateway:latest
    build:
      context: ./follow-image-gateway
      dockerfile: Dockerfile
      additional_contexts:
        follow-pkg: ./follow-pkg
    container_name: ${GATEWAY_CONTAINER_NAME:-follow-image-gateway}
    restart: unless-stopped
    depends_on:
      valkey:
        condition: service_healthy
      minio:
        condition: service_healthy
      createbuckets:
        condition: service_completed_successfully
    environment:
      - GATEWAY_SERVER_HOST=0.0.0.0
      - GATEWAY_SERVER_PORT=8090
      - IMG_GW_VALKEY_ADDRESSES=valkey:6379
      - MINIO_ENDPOINT=minio:9000
      - MINIO_ACCESS_KEY_ID=${MINIO_ACCESS_KEY_ID}
      - MINIO_SECRET_ACCESS_KEY=${MINIO_SECRET_ACCESS_KEY}
      - MINIO_USE_SSL=${MINIO_USE_SSL}
      - MINIO_BUCKET_NAME=follow-images
      - FOLLOW_API_ED25519_PUBLIC_KEY=${FOLLOW_API_ED25519_PUBLIC_KEY}
    ports:
      - "${GATEWAY_HOST_PORT:-8090}:8090"
    networks:
      - internal
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "5"
    deploy:
      resources:
        limits:
          memory: 1792m
          cpus: "2.0"
    healthcheck:
      # Use GET (-O /dev/null), not --spider: --spider sends HEAD, which
      # Goa-generated routes reject with 405 since /health is GET-only.
      test: ["CMD", "wget", "-q", "-O", "/dev/null", "http://localhost:8090/health"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 5s

# ── Volumes ────────────────────────────────────────────────────────

volumes:
  postgres_data:
    driver: local
  valkey_data:
    driver: local
  minio_data:
    driver: local

# ── Networks ───────────────────────────────────────────────────────

networks:
  internal:
    driver: bridge
    name: ${NETWORK_NAME:-follow-internal}
